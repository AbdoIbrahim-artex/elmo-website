/** @type {import('next').NextConfig} */
const nextConfig = {
  // output: 'export',
  reactStrictMode: true,
  swcMinify: true,
  // i18n: {
  //   locales: ['en', 'it'],
  //   defaultLocale: 'en',
  //   localeDetection : true,
  // },
}

module.exports = nextConfig
